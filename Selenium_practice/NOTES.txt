# NOTES
###################################

# Note: Always prefer id , css and then after all of this if you still cannot find the id and css then only use the Xpath.

## visibility_of_all_elements_located : 
-> Find the elements AND make sure they're visible.

## presence_of_all_elements_located :
-> Just make sure the elements exist in the page.
###################################



###################################
## ASSERTION

Syntax : assert <condition>

Think of assert as a checkpoint.

It asks:

"Is this condition true?"

✅ If yes, the test continues.
❌ If no, the test immediately fails with an AssertionError.

Its syntax is:

assert condition

or

assert condition, "Custom error message"
###################################




###################################
## IMPLICIT WAIT

. Selenium has a built-in way to automatically wait for elements called an implicit wait.
. An implicit wait value can be set either with the timeouts capability in the browser options,or with a driver method
. This is a global setting that applies to every element location call for the entire session.
. The default value is 0, which means that if the element is not found, it will immediately return an error.
. If an implicit wait is set, the driver will wait for the duration of the provided value before returning the error.
###################################



###################################
In console to verify the element is selected in case of no selectors hub:
$x(relative xpath)
$(css path)
####################################



####################################

PYTEST:

-> Pytest file should always start with test_ or end with _test

-> Methods name should always start with test_

-> Any code should be wrapped up in method.

######################################

COMMANDS IN PYTEST:

# py.test -v -s   (shows detailed information about each test (name + PASS/FAIL).)

# py.test -s  (Same as above + shows print() output from your tests.)

# py.test <file name> -v -s (Run for specific file only)
# E.g py.test test_demo1.py -v -s 

# Prefer This over smoke one :
# py.test -k <module name> -v -s (Selected Module Test Case )
# E.g  py.test -k CreditCard -v -s 


# -k stands for method names execution, 
# -s logs in out put -v stands for more info metadata
# Note: you can run specific file with py.test <filename> 


# you can run specific file with py.test <filename>

# @pytest.mark.smoke:  is used to group related tests so you can run only those tests when needed.
## E.g. pytest -m smoke

# you can skip tests with : @pytest.mark.skip


# Running but not reporting such as pass or fail : @pytest.mark.xfail


# In pytest, a fixture is a function that prepares something your test needs before the test runs.
# Fixture: A function used to provide setup and cleanup for tests.
## Created using @pytest.fixture().

# yield = pause fixture, let the test run, then continue with cleanup.


# Basically the fixture is used to invoke the browser or some configuration properties while the yield to exit the browser or delete the cookies at the end.


# Conftest -: Only add a particular function with fixture when multiple files will share that particular function.

#######################################